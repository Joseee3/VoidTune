<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VoidTune" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textAppearanceButton">@style/CustomButtonStyle</item>
        <item name="android:textColorPrimary">#FFFFFF</item> <!-- Color blanco para el texto -->
    </style>

    <!-- Estilo personalizado para los botones -->
    <style name="CustomButtonStyle" parent="TextAppearance.MaterialComponents.Button">
        <item name="textAllCaps">false</item>
    </style>
</resources>